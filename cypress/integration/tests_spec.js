describe('My First Test', () => {
  it('Does not do much!', () => {
    // cy.visit('https://joshpitzalis.com')
    // expect(true).to.equal(true)
    // cy.get('a').contains('See More Work').click()
    // cy.url().should('include', '/now')
    expect(true).to.equal(true)
  })
})
