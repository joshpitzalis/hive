language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
before_install:
  - npm install
  - npm install -g cypress-cli
before_script:
  - yarn start
script:
  - cypress run --record
