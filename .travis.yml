language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
    - /home/travis/.cypress/Cypress
install:
  - npm install
  - npm install -g cypress-cli
before_script:
  - npm start -- --silent &
script:
  - cypress run --record
